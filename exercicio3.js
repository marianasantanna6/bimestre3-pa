let nota1= 5
let nota2= 7
let media= (nota1= nota2)/2

if (media >= 7) {
    console.log (`Você está aprovado(a) com a média ${media}`);

} else {
    console.log (`Você está reprovado(a) com a média ${media}. Você precisa estudar mais!.` ); 
}